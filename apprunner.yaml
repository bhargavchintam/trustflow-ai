version: 1.0
runtime: docker
build:
  commands:
    build: []
run:
  runtime-version: latest
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
